//!  Auto Type Phrase
const typed = new Typed(".auto-input", {
  strings: ["We do everything nice nice for the right price!"],
  typeSpeed: 75,
  // backSpeed: 25,
  // delay: 1000,
  loop: false,
});
